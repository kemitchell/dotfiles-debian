#!/bin/bash

data="$HOME/todo"

if [[ ! -d "$data" ]]; then
  git clone git@github.com:kemitchell/todo "$data"
fi

todo.sh "$@"

cd "$data"

if ! git diff-index --quiet HEAD --; then
  git add .
  git commit --allow-empty-message -m ''
  git push origin master
fi
