[general]
accounts = kemitchell
ui = ttyui
pythonfile = ~/.offlineimap.py

[Account kemitchell]
localrepository = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/mail/kemitchell
status_backend = sqlite

[Repository Remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteuser = kylemitchell@fastmail.fm
remoteport = 993
holdconnectionopen = yes
ssl = yes
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
maxconnections = 5
realdelete = no
remotepasseval = subprocess.check_output(["pass", "show", "e-mail/kylemitchell@fastmail.fm"]).strip()
folderfilter = lambda folder: folder not in ['Deleted Items']
nametrans = lambda folder: re.sub('^INBOX\.', '', folder)
