#!/bin/sh
if ! command pup; then
  if command installpup; then
    installpup
  else
    echo "Install ericchiang/pup first."
    exit 1
  fi
fi

curl -s "https://git-scm.com/" \
  | pup '.version text{}' \
  | tr -d '[:space:]'
