#!/usr/bin/zsh
CACHE="$HOME/.ghmyrepos"

if [[ ! -f "$CACHE" || $* == *--refresh* ]]
then
  ghmyrepos > "$CACHE"
  if test -f "$HOME/.repos"; then
    cat "$HOME/.repos" >> "$CACHE"
  fi
fi

cat "$CACHE" | selecta | xargs hub clone
