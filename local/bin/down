set -e

if connectedtointernet; then
  pushrepos
  dotpushall
  if [ ! -f "$TRAVEL_FILE" ]; then
    purgefiles
  fi
  sudo shutdown now -h
else
  if promptyorn "No Internet connection. Shut down?"; then
    sudo shutdown now -h
  fi
fi
