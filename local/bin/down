#!/bin/sh
set -e

if connected-to-internet; then
  push-repos
  dot-push-all
  [ ! -f "$HOME/.traveling" ] && purge-files
  sudo shutdown now -h
else
  if prompt-y-or-n "No Internet connection. Shut down?"; then
    sudo shutdown now -h
  fi
fi
