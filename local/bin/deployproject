#!/usr/bin/zsh
if ! onmaster ; then
  echo "Error: not on master branch" >/dev/stderr
  exit 1
fi
if [ -x "./publish" ]; then
  promptyorn "Publish?" && ./publish
  exit
fi
if [ -x "./deploy" ]; then
  promptyorn "Deploy?" && ./deploy
  exit
fi
