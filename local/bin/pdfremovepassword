#!/bin/sh
pdf="$1"
password="$2"
if [ -z "$pdf" ] || [ -z "$password" ]; then
  echo "Usage: <file.pdf> <password>"
  exit 1
fi
basename=$(basename "$pdf" ".pdf")
pdftk "$1" input_pw "$password" output "$basename-no-passworc.pdf"
