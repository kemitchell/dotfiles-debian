#!/bin/sh
set -e
set -x

hash asdf || installasdf

hash dotsyncall && dotsyncall

for language in apt ruby rust asdf vimplugins nvimplugins git; do
  updater="update$language"
  hash "$updater" && "$updater"
done

for refreshscript in ~/.local/bin/refresh*; do
  "$refreshscript"
done
