[general]
accounts = kemitchell
ui = ttyui
pythonfile = ~/.offlineimap.py

[Account kemitchell]
localrepository = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/mail/kemitchell
status_backend = sqlite

[Repository Remote]
type = IMAP
remotehost = outlook.office365.com
remoteuser = kyle@kemitchell.com
ssl = yes
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
maxconnections = 4
realdelete = no
remotepasseval = subprocess.check_output(["pass", "show", "e-mail/kyle@kemitchell.com"]).strip()
folderfilter = lambda folder: folder not in ['Deleted Items', 'Tasks', 'Contacts', 'Tasks/Groceries', 'Tasks/Shopping', 'Junk', 'Calendar']
